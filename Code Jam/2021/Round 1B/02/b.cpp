#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;

    for (int k = 0; k < t; k ++) {
        int n;
        cin >> n;
        vector<int> units;
        for (int i = 0; i < n; i ++) {
            int tmp;
            cin >> tmp;
            units.push_back(tmp);
        }

        int cnt = 0;

        for (int i = 0; i < n; i ++) {
            
        }
    }
}