#include <bits/stdc++.h>

using namespace std;

int main() {
    int c;
    cin >> c;
    if (c - 2 > 0 && (c - 2) % 2 == 0) {
        cout << "YES";
    } else {
        cout << "NO";
    }

    return 0;
}